# Secrets
.env

# Real data dumps (S7-03 migration) — never committed, always deleted
# after the migration this ticket performs is verified complete.
*.pgdump
*.sql
*.dump

# Terraform local state and working files
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
*.tfplan
crash.log
crash.*.log
