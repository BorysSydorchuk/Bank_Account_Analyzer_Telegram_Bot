google-genai>=1.0.0
python-telegram-bot>=20.0
requests>=2.31.0
rich>=13.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
# S6-04: pydantic's EmailStr type needs this at runtime (import-time succeeds
# without it — pydantic only raises when EmailStr is actually used in a model).
email-validator>=2.0.0
PyJWT>=2.0.0
cryptography>=41.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.2.0
alembic>=1.13.0
anthropic>=0.40.0
celery[redis]>=5.4.0
slowapi>=0.1.9
passlib[bcrypt]>=1.7.4
# Pinned <4.1: passlib 1.7.4's bcrypt backend reads bcrypt.__about__.__version__,
# which bcrypt>=4.1 removed — unpinned, every password hash/verify call logs a
# spurious "error reading bcrypt version" warning (a known passlib/bcrypt
# compatibility gap, not a bug in this app).
bcrypt>=4.0.1,<4.1.0
